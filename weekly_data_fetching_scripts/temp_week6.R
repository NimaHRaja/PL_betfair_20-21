# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051483	


while (Sys.time() < "2020-10-23 21:50:00 BST"){
    
    AVLLEE <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051483))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(AVLLEE,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 300)
}

###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051243	


while (Sys.time() < "2020-10-24 14:20:00 BST"){
    
    WHUMCI <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051243))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(WHUMCI,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 180)
}

###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051320	


while (Sys.time() > "2020-10-24 14:40:00 BST" & Sys.time() < "2020-10-24 17:20:00 BST"){
    
    FULCRY <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051320))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(FULCRY,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 180)
}

###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051509	


while (Sys.time() < "2020-10-24 19:20:00 BST"){
    
    MUNCHE <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051509))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(MUNCHE,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 80)
}

###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051596	


while (Sys.time() < "2020-10-24 21:50:00 BST"){
    
    LIVSHU <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051596))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(LIVSHU,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 80)
}
###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051241	


while (Sys.time() < "2020-10-25 15:50:00 BST"){
    
    SOUEVE <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051241))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(SOUEVE,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 80)
}

###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051510	


while (Sys.time() < "2020-10-25 18:25:00 BST"){
    
    WOLNEW <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051510))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(WOLNEW,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 80)
}

###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051486	


while (Sys.time() < "2020-10-25 21:15:00 BST"){
    
    ARSLEI <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051486))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(ARSLEI,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 100)
}

###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051314	


while (Sys.time() < "2020-10-26 19:20:00 BST"){
    
    BRIWBA <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051314))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(BRIWBA,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 100)
}

###############################

# my_bf$events(filter = marketFilter(competitionIds = 10932509)) %>% View() # 30051345	


while (Sys.time() < "2020-10-26 21:50:00 BST"){
    
    BURTOT <- 
        my_bf$marketCatalogue(maxResults = 100, filter = marketFilter(eventIds = 30051345))
    
    PL <- 
        my_bf$marketCatalogue(maxResults = 1000, filter = marketFilter(eventIds = 2022802))
    
    
    lapply(BURTOT,get_and_save_a_market, data_folder)
    lapply(PL,get_and_save_a_market, data_folder)
    
    print(Sys.time())
    Sys.sleep(time = 60)
}

###############################


Sys.sleep(time = 300)